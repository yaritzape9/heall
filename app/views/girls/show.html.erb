<h2>Hey <%= @girl.username %>!</h2>
<p>Come talk to professional who can help solve your questions!
</p>

<% @girl.volunteers.each do |volunteer| %>
  <% if Message.where(girl_id: @girl.id, volunteer_id: volunteer.id).last %>
    <p><a href="/volunteers/<%=volunteer.id%>/girls/<%=@girl.id%>/messages">Talk to <%= volunteer.username %></a>, focused on <%= volunteer.expertises.first.name %> and <%= volunteer.expertises.last.name %></a>
      <% if (Message.where(girl_id: @girl.id, volunteer_id: volunteer.id).last.created_at) %>
        <small>Last talk at <%= Message.where(girl_id: @girl.id, volunteer_id: volunteer.id).last.created_at %>
        </small>
      <% else %>
      <small>You guys didn't start your conversation! What about telling your Ally about your hobbies to break the ice? :)
      </small>
      <% end %>
  <% end %>
<% end %>


<div class="padding:103px;">
<h3 style="margin:22px"> Need more help? </h3>

  <form action="/new-heally" method="POST" style="margin:22px">
    <select name="expertise">
      <option value="Pregnancy Prevention" name="Pregnancy Prevention">Pregnancy Prevention</option>
      <option value="STDs" name="STDs">STDs</option>
      <option value="Pregnant" name="Pregnant">Pregnant</option>
      <option value="Puberty" name="Puberty">Puberty</option>
      <option value="Rape Prevention" name="Rape Prevention">Rape Prevention</option>
      <option value="Mental Health" name="Mental Health">Mental Health</option>
    </select>
    <br>
    <br>
    <button type="submit">Find your next Ally</button>
  </form>
</div>
